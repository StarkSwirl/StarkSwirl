use sncast_std::{call, CallResult};
use core::fmt::Error;


// The example below uses a contract deployed to the Sepolia testnet
pub fn get_public_input_hash() -> Result<felt252, Error> {
    Result::Ok(1)
}
